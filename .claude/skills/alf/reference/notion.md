# 노션 갱신 (진행순서 8번에서 읽는다)

**순서를 지킨다: 📊 추적 DB → 📈 추이 DB → 추적 페이지.** DB 둘이 먼저, 페이지가 나중이다.

## 어디에 무엇이 있나

- **📊 ALF 개선 추적 DB** — **모든 개선점의 현재 상태가 모이는 단일 출처.** `collection://e6e40c92-555c-4c6f-ac31-1f24a7517fa6` (`notion-query-data-sources`로 조회, `notion-update-page`로 행 수정, `notion-create-pages`로 행 추가)
  - 컬럼: `개선점`(title) · `회차` · `점검일` · `분류`(규칙 개선/도큐먼트·연동/데이터·분석/운영 확인) · **`상태`** · `배경 (왜)` · `확인 방법` · `다음 분석 결과` · `관련 지표/영향`
- **📈 회차별 판정 등급 추이 DB** — **회차별 숫자가 쌓이는 곳.** `collection://cb98911f-68df-48d8-979c-15f04fd66eed` (추적 페이지 「지금 상태」 안에 차트로 붙어 있다)
  - ⚠️ **14차(2026-09-07)에 분모·봇완결 정의가, 15차(2026-09-11)에 이탈·놓침 정의가 바뀌었다.** 계열이 다른 행을 차트의 꺾임으로 읽지 말 것([metrics.md](metrics.md)).
- **추적 페이지** — 본문은 `지금 상태` · `다음 회차에 할 일` · `살아있는 개선점(DB)` · `최신 회차 상세` 4블록. https://app.notion.com/p/38b72773c363801db00cd81a068ffdaf
  - 하위 **「회차 기록」** — 기준선(6/22)부터 직전 회차까지의 상세를 보관하는 접기식 아카이브(최신이 위). https://app.notion.com/p/3c172773c363816e97dfe6b73fd58e8a
  - ⚠️ 워크스페이스에 제목이 `… (1)`인 **복제본이 따로 있다**(2026-08-19 구조 초안용, 은주 요청으로 보존). **갱신은 반드시 위 원본 URL에 한다.**

## `상태` 5종 — 이 정의대로만 쓴다

| 상태 | 언제 쓰나 |
|---|---|
| `완료/해소` | **2회 연속** 재발 없음이 확인됨. 한 회차 0건으로는 못 준다 |
| `미해소` | 확인했는데 그대로거나 악화됨. 회차를 넘어 살아있는 것 |
| `계속 추적` | 조치는 했고 효과 판정이 아직인 것 + 사례가 없어 판정 못 한 것 |
| `부분 확인` | 여러 갈래 중 일부만 확인됨(나머지 갈래를 결과 칸에 명시할 것) |
| `미확인` | 제안만 하고 손 안 댐 · **추적이 끊긴 것** |

## 8-A. 📊 추적 DB 갱신 (필수·먼저)

상태의 단일 출처다. 여기가 안 맞으면 다음 회차에 또 항목이 새어나간다.

- **진행순서 5-1 확인 목록의 모든 행**에 대해 `상태`와 `다음 분석 결과`를 갱신한다 (`notion-update-page` + `update_properties`).
  - `다음 분석 결과`는 **기존 텍스트를 지우지 말고 아래에 이어 붙인다** — `기존내용\n[{N}차 {MM-DD}] {판정} — {근거·수치}`. 회차별 경과가 한 칸에 쌓여야 "몇 회째 이월인지"가 보인다.
  - 기존 텍스트가 필요하므로, 수정 전 그 행의 `다음 분석 결과`를 먼저 조회한다.
- **이번 회차 신규 개선점은 행을 추가**한다 (`notion-create-pages`, parent는 `data_source_id`). `회차`·`점검일`·`분류`·`상태`·`배경 (왜)`·`확인 방법`·`관련 지표/영향`을 채운다.
  - `확인 방법`은 **"다음에 무엇을 몇 건에서 몇 건으로 세면 됐는지 알 수 있나"**로 숫자 기준선을 박아 쓴다. 이게 흐릿하면 다음 회차에 판정이 안 선다.
  - 조치를 막 한 것은 `계속 추적`, 제안만 하고 안 고친 것은 `미확인`.
- **`회차` select에 이번 회차 옵션이 없으면 먼저 추가**한다(`notion-update-data-source`). 없다고 빈칸으로 두지 말 것.
- **행을 지우지 않는다.** 해소된 것도 `완료/해소`로 남긴다 — 뒤집힌 전례가 있어 이력이 근거가 된다([past-cases.md](past-cases.md)).

## 8-B. 📈 추이 DB에 이번 회차 행을 추가한다 (필수)

`collection://cb98911f-68df-48d8-979c-15f04fd66eed` (`notion-create-pages`, parent는 `data_source_id`)

🔴 **매 회차 빠뜨리지 말 것.** 은주가 여러 번 따로 요청했던 부분이다 — 페이지의 「지금 상태」만 고치고 이 DB를 안 채우면 **차트가 회차 하나씩 비어 추이가 끊긴다.**

채울 칸 (전부 숫자, 비율은 **소수로** — 60.5%는 `0.605`):

| 칸 | 값 |
|---|---|
| `회차`(title) · `회차 복사` | `14차` · `14` |
| `점검일` | 점검한 날 |
| `신규 분석` | 중복 제거 후 신규 건수 |
| `ALF 미발동` | `alfTriggered=false` 건수 |
| `분모` | `alfTriggered=true` 건수 |
| `봇완결` · `봇완결률` | 미배정 건수 · 미배정÷분모(소수) |
| `이탈` · `이탈률` | 이탈 건수 · 이탈÷분모(소수) |
| `없앨수있는이관` · `2-A 안내로 가능` · `2-B 조회·실행 필요` | 2-A+2-B · 2-A · 2-B |
| `정당이관` · `놓침` | 3계열 합 · 오안내+이관안함+안내안함 |
| `이 회차에 무슨 일이` | 4~6줄 요약(최대 발견·닫은 것·되살아난 것·분모 주의) |

- ⚠️ **정의가 바뀐 회차면 `이 회차에 무슨 일이` 첫 줄에 그 사실을 쓴다.** 이 DB는 차트로 그려지므로 정의가 바뀐 지점을 표시하지 않으면 꺾인 선을 개선/악화로 오독한다.
- 저장 후 **행 수가 회차 수와 같은지** 확인한다. 비어 있는 회차가 있으면 그때 채운다.

## 8-C. 추적 페이지 갱신 — 본문 4블록 + 하위 아카이브

페이지 구조(2026-08-19 정리, 이 순서와 블록 수를 바꾸지 않는다):
`지금 상태`(최신 회차 4줄 요약 + 핵심 지표 표 + 측정 규칙 + 📈 차트 DB) → `다음 회차에 할 일` → `살아있는 개선점`(📊 추적 DB) → `최신 회차 상세`(접기) → 맨 아래 `회차 기록` 하위 페이지 링크

회차가 늘 때 하는 일은 **네 가지**다.

**1. 직전 회차 상세를 아카이브로 내린다.** 본문의 「N-1차 상세」 토글 블록을 통째로 잘라, 하위 페이지 [회차 기록](https://app.notion.com/p/3c172773c363816e97dfe6b73fd58e8a)의 **맨 위**에 붙인다(`notion-update-page` + `insert_content`, `position: {"type":"start"}`). 아카이브는 최신이 위다.
- 옮길 때 제목을 `# N차 점검 (YYYY-MM-DD) 개선점 추적 {toggle="true"}` 형식으로 바꾸고, **본문 전체를 탭 한 칸 들여쓴다**(들여쓰지 않으면 토글 밖으로 나온다).
- **본문에서 지우는 것과 아카이브에 붙이는 것을 같은 턴에 한다.** 지우기만 하고 붙이기를 잊으면 회차가 통째로 사라진다.

**2. 본문에 이번 회차 상세를 넣는다.** 제목은 `# N차 상세 (YYYY-MM-DD) {toggle="true"}`, 하위 소제목은 **네 개로 고정**한다 — `## 분석 결과` / `## 이번 회차 발견` / `## 안 풀린 것 · 개선 확인된 것` / `## 개선점 추적 (N차 신규)`. **소제목을 늘리지 않는다**(11차 전까지 회차당 8개까지 늘어 뭐가 중요한지 안 보였다).
- `분석 결과` = 판정 등급 표 + 검증 줄 + `AI상담/*` 태그 분포.
- `이번 회차 발견` = 최대 발견 1~3개를 수치·chatId와 함께. 봇 발화가 있으면 원문을 인용한다.
- `개선점 추적` = `개선점 / 배경 (왜) / 확인 방법 / 상태` 4열 표. **이번 회차 신규만** 넣는다(기존 항목 경과는 DB가 관리).

**3. 「지금 상태」를 이번 회차로 갈아끼운다.** 제목의 회차·날짜, 4줄 요약, 핵심 지표 표의 `N차 기준선`·`직전 흐름` 두 열.

**4. 「다음 회차에 할 일」을 새로 쓴다.** `먼저 볼 것`(5개 이내) / `계속 확인` / `이월·대기` 세 묶음.
- 🔴 **직전 회차 목록을 그대로 복사해 오지 말 것.** 항목마다 숫자를 **이번 회차 값으로 갈아끼우고**, 종결된 항목은 지운다(11차 사례 → past-cases.md).
- 미해결 전건은 DB가 관리한다. 여기엔 다 옮기지 않는다(이중 관리).
- `이월·대기` 마지막 줄에 항상 `DB에서 상태 = 미확인 을 걸러본다. 회차당 최소 1건은 결론 낼 것`을 남긴다.

## 지킬 것

- ⚠️ **본문에 회차 섹션을 쌓지 말 것.** 2026-08-19에 2~10차 서술 11개(1,037줄)를 하위 페이지로 내렸다. **본문에 남는 회차는 항상 최신 하나뿐**이다.
- ⚠️ **상태 표를 페이지에 다시 만들지 말 것.** 2026-08-11에 「누적 해소 현황」 표를 DB로 합치고 페이지에서 지웠다. 표를 또 만들면 DB와 어긋난다.
- ⚠️ **원문자(①②③, ⓐⓑ, ⑨⑩⑪)를 쓰지 말 것 — 은주 화면에서 안 보인다.** 판정 등급은 이름으로 부르고(`봇완결`·`정당 이관`·`놓침`·`이탈`), 자동화 여력은 `2-A`/`2-B`로 쓴다. ※ 아카이브에 이미 들어간 과거 회차는 원문 보존이라 그대로 둔다.
- **강조를 아껴 쓴다.** 굵게는 문단·표 셀당 1개까지, 인용문(`>`)은 경고·검증처럼 층이 다른 정보에만.
- 🔴 **`<database>`·`<page>` 태그를 지우지 말 것.** `replace_content`로 본문을 통째로 바꿀 때 이 세 줄(📈 차트 DB · 📊 추적 DB · 회차 기록 하위 페이지)이 빠지면 **실제로 삭제된다.** 교체 전 `notion-fetch`로 현재 태그의 `url`·`data-source-url`을 그대로 복사해 둔다.
- 페이지 표 수정 시 `update_content`의 `old_str`은 **들여쓰기까지 정확히** 맞아야 한다. HTML 주석(`<!-- -->`)은 마커로 쓰지 말 것(매칭 안 됨).
- 개인정보는 노션에도 옮기지 않는다(익명화).
